import React from "react";

function Homepage() {
    return(
        <div className="flex justify-center bg-[#64705C] h-screen w-full p-10 m-9 rounded-lg">   
            <h1>Building these things tomorow</h1>
            <h1>style background color</h1>
            <h1>integrate weather api and create a display for weather https://open-meteo.com/</h1>
            <h1>when using api let user input their location which would be saved to their instance variable</h1>
            <h1>After everytime the user logs in the weather api should use their location given from backend to load the correct weather based off of the location associated to them</h1>
            <h1>instead of button to move nav bar have it move when hovered over</h1>
        </div>
    )
}

export default Homepage