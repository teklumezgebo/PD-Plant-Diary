import React from "react";

function Homepage() {
    return(
        <div>    
            <h1>Building these things tomorow</h1>
            <ul>
                <li>style background color</li>
                <li>integrate weather api and create a display for weather https://open-meteo.com/</li>
                <li>when using api let user input their location which would be saved to their instance variable</li>
                <li>After everytime the user logs in the weather api should use their location given from backend to load the correct weather based off of the location associated to them</li>
                <li>instead of button to move nav bar have it move when hovered over</li>
            </ul>
        </div>
    )
}

export default Homepage